---
# linux_lib defaults

# Best practice is to include vars_from ansible_os_family
# - include_role:
#     name: vbotka.linux_lib
#     tasks_from: <File to load from a role's tasks/ directory>
#     vars_from: "{{ ansible_os_family }}"

ll_debug: false
ll_backup_conf: false

# install_package
ll_ipkg_sanity: true
ll_ipkg_sanity_quiet: true
ll_ipkg_retries: 5
ll_ipkg_delay: 2
ll_ipkg_rescue_end_host: true

# EOF
...
